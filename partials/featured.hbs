{{#get "posts" filter="featured:true" limit="all" as |featured|}}
    {{#if featured}}
        <section class="featured-wrapper container">
            <h2 class="featured-title">Features</h2>
            <div class="featured-feed owl">
                {{#foreach featured visibility="all"}}
                    <article class="{{post_class}} u-text-format">
                        {{#if feature_image}}
                            <div class="u-placeholder horizontal">
                                <img class="lazyload u-object-fit"
                                    data-srcset="{{> srcset}}" data-sizes="auto"
                                    src="{{img_url feature_image size="m"}}"
                                    srcset="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="
                                    alt="{{title}}">
                            </div>
                        {{/if}}
                        <h2 class="post-title u-text-format">{{title}}</h3>
                        {{!-- <a class="u-permalink" href={{url}}
                            aria-label="{{title}}"></a> --}}
                        {{content}}
                    </article>
                {{/foreach}}
            </div>
        </section>
    {{/if}}
{{/get}}